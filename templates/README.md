# Шаблоны отображения планов питания

В этой папке находятся 3 стиля отображения планов питания для мини-приложений Telegram.

## Доступные стили:

### 1. Modern Style (plan_style_modern.html)
- **Дизайн**: Современный градиентный фон с яркими карточками
- **Цветовая схема**: Фиолетовые градиенты (#667eea → #764ba2)
- **Особенности**:
  - Плавные анимации появления
  - Крупные иконки приемов пищи
  - Градиентные бейджи калорий
  - Grid-сетка для статистики

### 2. Minimalist Style (plan_style_minimalist.html)
- **Дизайн**: Минималистичный черно-белый стиль
- **Цветовая схема**: Черный (#1a1a1a) на белом фоне
- **Особенности**:
  - Простые линии и много белого пространства
  - Табличное отображение ингредиентов
  - Легкость чтения и элегантность
  - Четкая типографика

### 3. Colorful Style (plan_style_colorful.html)
- **Дизайн**: Яркий, веселый, с анимациями
- **Цветовая схема**: Разноцветные градиенты (красный, желтый, голубой, розовый)
- **Особенности**:
  - Анимированный градиентный фон
  - Карточки с легким поворотом
  - Множество эмодзи
  - Bounce-анимации
  - Эффекты при наведении

## Как использовать:

### Способ 1: Отправка через Telegram Mini App
```python
# В боте после генерации плана:
html_content = open('templates/plan_style_modern.html').read()
# Замените данные в шаблоне на реальные
html_content = html_content.replace('{{meal_name}}', meal_name)
# И т.д.

# Создайте Web App URL или отправьте через inline button:
keyboard = InlineKeyboardMarkup([[
    InlineKeyboardButton("📱 Открыть план", web_app=WebAppInfo(url="your_url"))
]])
```

### Способ 2: Сохранение и отправка как HTML файл
```python
import os
from telegram import Update

async def send_plan(update: Update, plan_data):
    # Генерируем HTML из шаблона
    with open('templates/plan_style_modern.html', 'r', encoding='utf-8') as f:
        html_template = f.read()

    # Заполняем данными
    html_filled = html_template  # Добавьте логику замены данных

    # Сохраняем временно
    temp_file = f'temp_plan_{update.effective_user.id}.html'
    with open(temp_file, 'w', encoding='utf-8') as f:
        f.write(html_filled)

    # Отправляем
    await update.message.reply_text(
        "✅ Ваш план готов! Чтобы посмотреть, откройте мини-приложение",
        reply_markup=InlineKeyboardMarkup([[
            InlineKeyboardButton("📱 Открыть план",
                               web_app={"url": f"https://yourserver.com/{temp_file}"})
        ]])
    )
```

### Способ 3: Интеграция с сервером
Разместите HTML-файлы на веб-сервере и передавайте параметры через URL:
```
https://yourserver.com/plan.html?style=modern&user_id=123&plan_id=456
```

## Кастомизация:

Каждый шаблон можно легко настроить:

1. **Цвета**: Измените CSS переменные в секции `<style>`
2. **Шрифты**: Замените `font-family` на нужный
3. **Анимации**: Добавьте или удалите `@keyframes` правила
4. **Структура**: Добавьте дополнительные блоки (например, перекусы)

## Примечания:

- Все шаблоны адаптивны (responsive)
- Поддерживают мобильные устройства
- Не требуют внешних библиотек (pure HTML/CSS)
- Можно легко расширить для планов тренировок

## Пример замены данных в шаблоне:

```python
def fill_template(template_path, plan_data):
    with open(template_path, 'r', encoding='utf-8') as f:
        html = f.read()

    # Замените плейсхолдеры на реальные данные
    replacements = {
        '{{breakfast_name}}': plan_data['breakfast']['name'],
        '{{breakfast_calories}}': plan_data['breakfast']['calories'],
        # И так далее...
    }

    for placeholder, value in replacements.items():
        html = html.replace(placeholder, str(value))

    return html
```

---

**Создано для фитнес-бота с AI-помощником** 🤖💪
